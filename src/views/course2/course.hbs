<style>
</style>

<nav aria-label="breadcrumb">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="/">Acasă</a></li>
        <li class="breadcrumb-item"><a href="/cursuri2/">Cursuri</a></li>
        <li class="breadcrumb-item active" aria-current="page">curs {{course.code}}</li>
    </ol>
</nav>

<h1>Curs {{course.code}} <span class="font-weight-light text-secondary h4"> - {{course.name}}</span>
</h1>
{{!-- <h2 class="mb-5">{{class.name}}</h2> --}}


{{!-- <h3 class="mt-5" {{#if noCourse}} class="text-muted" {{/if}}>{{course.dateAsString}}</h3> --}}



<div class="h4 font-weight-light">{{course.description}}</div>

Clasa: {{course.grade}} </br>
Nivel: {{course.level}} </br>
Durata: {{course.duration}} </br>
</br></br>
<h5>Agenda:</h5>

{{!-- {{#each course.agenda.areas}}
<div class="font-weight-bold">{{name}}</div>
<ul>
    {{#each chapters}}
    <li>
        {{name}}
    </li>
    <ul>
        {{#each sections}}
        <li>
            {{name}}
        </li>
        {{/each}}
    </ul>
    {{/each}}
</ul>
{{/each}} --}}

{{#each lessonsTree}}
<div class="font-weight-bold">{{@key}}</div>
<ul>
    {{#each this}}
    <li>
        {{@key}}
    </li>
    <ul>
        {{#each this}}
        <li>
            <a href="/lectii/{{_id}}">{{title}}</a>
        </li>
        {{/each}}
    </ul>
    {{/each}}
</ul>
{{/each}}


{{!-- {{#each course.videos}}
<div class="mt-2">Partea {{inc @index}}:</div>
<div class="responsive">
    <iframe width="560" height2="315" src="{{url}}" frameborder="0"
        allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture" allowfullscreen>
    </iframe>
</div>
{{/each}}

{{#if course.videos}}
<p class="h4 mt-5 mb-1">Varianta scrisă:</p>
{{/if}}

{{#each course.images}}
<p>{{course.description}}</p>
<img src={{url}} class="img-fluid" alt="">
<a style="display: block;" href={{highQualityUrl}}>Vezi poza la calitate maximă</a>
{{/each}}
<br>
<br> --}}